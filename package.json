{
  "name": "nodejs-jenkins-app",
  "version": "1.0.0",
  "description": "A simple Node.js application to test Jenkins CI/CD.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

